[project]
name = "nvmetcp-tls-fuzz"
version = "0.1.0"
description = "Grammar-aware NVMe/TCP TLS protocol field fuzzing harnesses"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
nvmetcp-tls-fuzz = "nvmetcp_tls_fuzz.cli:main"
